/*public class PolynomialController implements Controller {
    private final View view;
    private final Model model;

    public PolynomialController(View view, Model model) {
        this.view = view;
        this.model = model;
    }

    /*@Override
    public void addition() {
        Polynomial p1 = view.getPolynomialInput();
        Polynomial p2 = view.getPolynomialInput();
        Polynomial result = model.addition(p1, p2);
        view.display(result.toString());
    }

    @Override
    public void subtraction() {
        Polynomial p1 = view.getPolynomialInput();
        Polynomial p2 = view.getPolynomialInput();
        Polynomial result = model.subtract(p1, p2);
        view.display(result.toString());
    }

    @Override
    public void multiplication() {
        Polynomial p1 = view.getPolynomialInput();
        Polynomial p2 = view.getPolynomialInput();
        Polynomial result = model.multiply(p1, p2);
        view.display(result.toString());
    }
}*/