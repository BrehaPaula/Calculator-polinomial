//import java.awt.event.ActionEvent;
//import java.awt.event.ActionListener;
/*
import javax.swing.JButton;
//import javax.swing.JFrame;
import javax.swing.JLabel;
import javax.swing.JPanel;
import javax.swing.JTextField;
public class PolynomialOperationsViewImpl implements PolynomialOperationsView {

    private View view;

    public PolynomialOperationsViewImpl(String name) {
        view = new View(name);
        initialize();
    }

    @Override
    public void setAdditionListener(ActionListener listener) {
        // view.getButton().removeActionListener(controller);
        view.getButton().addActionListener(listener);

    }

    @Override
    public void setSubtractionListener(ActionListener listener) {
        // view.getButton().removeActionListener(controller);
        view.getButton().addActionListener(listener);
    }

    @Override
    public void setMultiplicationListener(ActionListener listener) {
        //  view.getButton().removeActionListener(controller);
        view.getButton().addActionListener(listener);
    }

    @Override
    public String getFirstPolynomial() {
        return view.getText().getText();
    }

    @Override
    public String getSecondPolynomial() {
        return view.getText().getText();
    }

    @Override
    public void setResult(String result) {
        view.getLabel().setText(result);
    }

    @Override
    public View getView() {
        return view;
    }

    public static void main(String[] args) {
        PolynomialOperationsView view = new PolynomialOperationsViewImpl("Polynomial Operations");
        JFrame frame = view.getView();
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.pack();
        frame.setVisible(true);
    }
}*/